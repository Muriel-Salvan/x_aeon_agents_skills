#!/usr/bin/env ruby

# Parameters:
# * *requirements* (String): The requirements to implement
# * *run_id* (String): [Optional] The run ID for persistence
requirements = ARGV[0]
run_id = ARGV[1]

require 'x_aeon_agents_skills'

XAeonAgentsSkills::Configuration.configure(**XAeonAgentsSkills::Helpers.keys_from_launcher)
XAeonAgentsSkills::Agents.implement_requirements(requirements, run_id:)
